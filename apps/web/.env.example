VITE_API_URL=http://localhost:3000
# Local dev only: the API's TENANT_HEADER_FALLBACK lets us select a tenant without real
# subdomains. Set to the slug of a tenant you created via POST /tenants.
VITE_DEV_TENANT_SLUG=
